import { useState } from 'react'; import { Card } from '../components/Card'; import { nextMonth, previousMonth } from '../utils/date'
export function CalendarPage(){const now=new Date(),[value,setValue]=useState({year:now.getFullYear(),month:now.getMonth()+1});return <><div className="eyebrow">月份紀錄</div><div className="month-switch"><button aria-label="上個月" onClick={()=>setValue(previousMonth(value))}>‹</button><h2>{value.year} 年 {value.month} 月</h2><button aria-label="下個月" onClick={()=>setValue(nextMonth(value))}>›</button></div><Card><p className="empty">此月份尚無工時紀錄</p></Card></>}
