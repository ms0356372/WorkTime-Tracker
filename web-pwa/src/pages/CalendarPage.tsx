import { useEffect,useState } from 'react';import { Card } from '../components/Card';import type { WorkRecord } from '../models/domain';import { recordsRepository } from '../repositories';import { nextMonth,previousMonth } from '../utils/date';import { calculateWorkMinutes } from '../services/workTimeService';import { formatMinutes } from '../utils/time'
export function CalendarPage({onEdit}:{onEdit:(record:WorkRecord)=>void}){const now=new Date(),[value,setValue]=useState({year:now.getFullYear(),month:now.getMonth()+1}),[rows,setRows]=useState<WorkRecord[]>([]);async function refresh(){setRows(await recordsRepository.recordsForMonth(value.year,value.month))}useEffect(()=>{void refresh()},[value]);return <><div className="eyebrow">月份紀錄</div><div className="month-switch"><button aria-label="上個月" onClick={()=>setValue(previousMonth(value))}>‹</button><h2>{value.year} 年 {value.month} 月</h2><button aria-label="下個月" onClick={()=>setValue(nextMonth(value))}>›</button></div><Card title="月份紀錄"><div className="item-list calendar-list">{rows.map(row=><div className="list-row" key={row.workDate}><div><strong>{row.workDate.slice(5).replace('-','/')}</strong><span>工時 {formatMinutes(calculateWorkMinutes(row))}</span></div><button className="small" onClick={()=>onEdit(row)}>修改</button>{row.id&&<button className="danger small" onClick={()=>void recordsRepository.delete(row.id!).then(refresh)}>刪除</button>}</div>)}{!rows.length&&<p className="empty">此月份尚無工時紀錄</p>}</div></Card></>}
